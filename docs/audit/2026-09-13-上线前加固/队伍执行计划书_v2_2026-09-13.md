# 队伍执行计划书 v2 · TempoSoul 上线前加固（T1-T4 + 主控收口）

> 版本：v2（2026-09-13，基于当日实态核查）｜ v1 = 各线程任务卡.md（原计划基线）
> 用途：**分派给其他智能体执行剩余任务**；本计划书以 v1 为基线，仅列「当前尚未完成」的工作，冲突处以本计划书为准。
> 总纲：`docs\audit\2026-09-13-上线前加固\README.md`（统一输出目录 / 共享规范 / WORKLOG 模板 / 状态标记）。
> 原则：诚实第一、先诊断后动手、完成即审计、交接零断点；**禁止任何智能体直接部署线上 / 合并 main / bump sw**（收口权在主控=豆包）。

---

## 〇、当前实态速览（2026-09-13 核查，分派依据）

| 队 | 已完成 | 剩余 | 线上实测 |
|---|---|---|---|
| T1 | S1-S2 规则审计 22/22 体系（A1 已修 @ ef0cb90）、边界声明 | **S3-S7 全部**（比对站/黄金样例/比对脚本/差异闭环/回归总结） | 未部署分支 |
| T2 | S1-S6 审计 ✅、M1 已部署上线（sw v6 / 4de2841d）、M2 第一批 318 条+214 语义库、M3 桥接层 | M2 人工核验、M3 全量接入（待 T1）、M4 语义熔断、红线 2.1 申报 | ✅ 已上线 |
| T3 | S1-S6 审计 ✅、M0-M4 代码全部完成（lang/术语表 1201 行/L0 查表/回译测试 10/10） | M2 尾 ZiweiBoard、BP1 引擎键化、L1/L3/L5 模板×7、回译 nightly、母语复核（人工） | 未部署 |
| T4 | 503 根因（1102 CPU 超限）+ 修复 3 commit（98cad2a/34da981/165a81f）、红线判定、shared 三件套 | **部署+线上复测**、第三片懒加载（可选）、CF 套餐确认、红线矩阵正式修订 | ⚠️ **ziwei 3 连测 1×200+2×1102，503 仍在发作** |

**关键结论**：T1 核心比对测试未做；T3/T4 代码完成但未部署（线上 1102 未消）；T2 已部分上线。**任何"全部完成"的宣告都不成立**，以下为分派依据。

---

## 一、执行总序与依赖

```
并行组 A（立即启动）
├─ 队1 T1-S3/S4（豆包：比对站清单在线核验 + Codex：黄金样例脚本化）
├─ 队2 T2-M2剩余（ZCode：AI 起草 L1 draft 批次 + 人工核验通道）
└─ 队3 T3-M2尾/BP1（Codex：ZiweiBoard 接入 + 引擎键化会签准备）

依赖链（关键路径）
└─ 队1 T1-S5 比对执行 ──依赖──> 队4 T4 部署（503 修复上线后才能线上复测）
└─ 队2 T2-M3 全量接入 ──依赖──> T1 十神/格局审计结论
└─ 队3 T3-BP1 引擎键化 ──依赖──> T1 键化会签 + T4 术语键格式（shared 已定 v1）

主控收口（最后，唯一有权部署/合并）
└─ 合并 T1/T2/T3/T4 分支 → bump sw → wrangler deploy → repro_503 验证 → 红线终裁 → 记忆/AI 地图
```

**执行者建议**：Codex=代码对勘/修复/脚本；ZCode=数据生产/词库；DSH=审计复核/回归；Hermes=在线核验/情报；豆包=主控收口+人工配合项协调。母语复核、命理顾问核验、CF 套餐确认为人工配合项（用户安排）。

---

## 二、队 1 · T1 排盘规则审计与比对测试（剩余 S3-S7）

### 当前状态
- ✅ S1 映射表（63 端点 ↔ 21+1 体系 ↔ 148 测试文件）；S2 规则审计 22/22 体系 + A1 修复（thread/t1-paipan-audit @ ef0cb90，112/112 绿）+ 边界支持范围声明（1900-2100）
- ❌ S3-S7 全部未做

### 目标
完成与各板块 top5 网站的同数据比对，输出逐字段 diff，**所有差异归因闭环（A 修复/B 文档化/D 标裁决）**，三套测试全绿。

### 剩余步骤
- **S3 比对站清单**（豆包在线，0.5 天）：12 排盘域（bazi/ziwei/qizheng/taiyi/huangji/wuyun/zodiac/bazhai/xuankong/residential/astrolabe/vedic-本地口径）+ 9 占卜域（liuyao/meihua/qimen/liuren/xiaoliuren/jinkoujue/tarot/lenormand/ssgw/almanac）。每体系选 5 站：在线可达性核验、支持参数确认、**口径核对清单**（真太阳时默认？子时口径？替卦？立春换年？）。文献型体系（taiyi/huangji/wuyun/zodiac/ssgw/tarot/lenormand）用「开源参照+典籍推演」双源替代；皇极须锁同一 epochYear。产出 `thread-01\output\比对站清单.md`。
- **S4 黄金样例集**（Codex，1 天）：每体系 ≥10 组 JSON，优先 zodiac/ssgw/taiyi/huangji/wuyun-liuqi（测试最薄）；边界限 1900-2100；复用 `_audit_20260912` 10 组；格式遵守 `shared\样例集schema.md`。产出 `thread-01\output\golden-cases\<体系>\<case-id>.json`。
- **S5 比对脚本与执行**（Codex，2-3 天）：扩展 `_audit_20260912\cross_check.py` → `compare_vs_sites.py`；本地引擎跑（T4 部署前线上 503 不稳）；输出 `{体系,case,字段,引擎值,网站值,一致?,差异类型}`；每站记录 URL+抓取时间；抓取受限只记「人工比对」不造假。产出逐体系 `比对测试报告_*.md`。
- **S6 差异闭环**（Codex+豆包，1-2 天）：A 类修复+单测；B 类（B1-B10 台账）文档化+产品明示；D 类（D1-D5）标专家裁决；H1-H4 工程卫生修复（贵人表双源/ssgw 死数据/六爻恒真断言/lunar 机器时区）。台账更新规则审计总表 v3。
- **S7 回归与总结**（DSH，0.5 天）：`test:core`/`test:api`/`test:prompt` 串行 ≥ 基线；产出 `审计总结.md` + 修复 commit 清单。

### 验收
每体系 ≥10 组 × 5 站比对完成可复现；字段一致率 100% 或差异全部归因闭环；0 未解释 5xx；三套测试全绿。

### 依赖 / 移交
- 依赖：无（本地引擎可跑）；线上复测待队 4 部署。
- 移交：十神/格局审计结论 → 队 2 M3 全量接入；键化会签结论 → 队 3 BP1。

### AI 地图查询（队 1 专用）
Read `AI地图\00_总览与排障速查.md`（如缺失降级）、`memory\_PROJECTS.md`、`AI地图\11_国学出海\主地图.md` → `memory\guoxue-overseas\context.md`+最近 3 日期文件 → `task_status.md` → 8877 `/memory/query`+`/corpus/query`（关键词：排盘 对勘 真太阳时 ziwei 503）→ 项目内 `_audit_20260912\`、`规则审计总表.md`。检索后一句话汇报。

### AI 地图更新（队 1 专用）
里程碑（清单/样例/比对报告/总结）完成后 6 步：分册部署日志 `11_国学出海\部署日志\2026-09-13_T1_<里程碑>.md` → 主地图 → `00_总览` → `_更新日志.md`（**UTF-16LE，用 Python utf-16-le 追加，禁 echo**）→ `POST /memory/write`（`[来源: T1 @ …]`, session_id=guoxue-overseas）→ `POST /corpus/scan`。

### 交接防断点（队 1 专用）
产物只落 `thread-01-排盘规则审计与比对\`；git 走 `thread/t1-paipan-audit`；每步写 WORKLOG；接手顺序 README→本计划书→WORKLOG→output；⚠️阻塞写清「尝试过什么/谁解锁」。

---

## 三、队 2 · T2 三级转译实施（剩余 M2/M3/M4 + 红线申报）

### 当前状态
- ✅ 审计 S1-S6 全部（24 项红线建议 ❌13/🟡6/na5）；M1 运行时约束**已部署上线**（sw v6，temperature=0+合规注入+OutputFuse，7/7 测试）；M2 第一批 318 条（L1 补齐 36 条六爻/梅花，dict 1.1.0）+ 214 条 semanticLibrary 全自动批次（dict 1.2.0）；M3 桥接层 `archetype-bridge.ts`（7/7 测试，provisional 待 T1 回填）
- ❌ M2 人工核验（214 条 L1 draft→verified）、M3 全量接入（待 T1）、M4 语义熔断+覆盖率看板、红线 2.1 正式申报

### 目标
转译管线数据+运行时可交付：L1 全量 verified、M3 全量接入生产 prompt、语义熔断与看板上线、2.1 红线申报材料齐备。

### 剩余步骤
- **M2 剩余**（ZCode+人工配合）：AI 起草 214 条 L1 古籍引文 draft 批次（可全自动，复用 m2-gen-semantic.py 模式：enable_thinking=false+限速重试+断点续传+禁词门禁）；**verified 状态必须人工核验**（命理顾问/用户安排，勿标 verified 冒充）；数据修正项（天罗地网/xuetang_yima 键错位）对照资产线源数据核正。dict_version → 1.3.0。
- **M3 全量接入**（Codex，待 T1 十神/格局结论）：`buildBaziPrompt` 集成 `buildArchetypeSection`（L1 引文+L3 白话口径注入段）；extractTenGodKeys provisional → 按 T1 结论回填 verified；其余体系（紫微/占卜）桥接扩展。测试 ≥10 用例。
- **M4 语义熔断与看板**（Codex，1 天）：OutputFuse 语义层（当前 5 组高危正则已落地，补语义盲区——本地 8080 实证发现推理模型 reasoning 吃 token 与禁词正则语义盲区）；覆盖率看板脚本（词库/模板/语言 × 红线项）。
- **红线 2.1 申报**（DSH，0.5 天）：按 v2 判定（❌13/🟡6/na5）产出逐项申报材料（证据/建议状态/缺口），交主控+用户终裁。

### 验收
318+214 条 L1 全 verified 或明确 pending_manual；M3 全量接入后 prompt 回归全绿；M4 看板可运行；2.1 申报材料可直接评审。

### 依赖 / 移交
- M3 依赖 T1 结论；M2 verified 依赖人工；M4 依赖 M3。
- 移交 T3：术语键 = archetype_key（已对齐 shared v1）、翻译源 = L3 白话。

### AI 地图查询 / 更新 / 交接（队 2 专用）
同队 1 三规范；关键词：转译 词库 L1 L3 L5 熔断；产物只落 `thread-02-三级转译审计与方案\`；git 走 `thread/t2-translation-audit`（已推，含 M1 部署记录 dc86ea1 在远程 main）。

---

## 四、队 3 · T3 多语言翻译实施（剩余 M2尾/BP1/模板/CI + 母语复核）

### 当前状态
- ✅ 审计 S1-S6（9 断点 BP1-BP9、口径三分法、术语表 1201 行 tier1 355、双轨管线方案）；M0 生成器 `scripts/i18n/gen-terms-7lang.py` + `src/data/terms-7lang.ts`；M1 proxy lang+mode:'translate'（4/4 测试）；M2 首片 BaziChartBoard L0 查表 shim；M3 服务端术语注入+门控（I18N_ENABLED_LOCALES）；M4 G1-lite 测试（10/10）。全绿 api 108/prompt 229，未部署。
- ❌ M2 尾 ZiweiBoard、BP1 引擎键化、L1/L3/L5 模板×7、回译 nightly、母语复核（人工）

### 目标
前端全链路 L0 查表落地、引擎输出键化、7 语言受限翻译模板齐备、回译入 CI、母语复核闭环。

### 剩余步骤
- **M2 尾**（Codex，0.5 天）：ZiweiBoard 接入 useTermLabel/TermText；**注意 AstrolabeBoard/QizhengBoard 有意不接**（同名不同义，正解=BP1 引擎键化），记录会签结论。
- **BP1 引擎键化**（Codex，1.5 天，待 T1 键化会签）：L0 结构化字段（干支/星曜/宫位/卦名）产出 archetype_key 化（对齐 shared v1 三段式 `bazi:stem:jia`）；这是「多语言 100% 确定性」的根，其余 Board 全部改查表渲染。
- **L1/L3/L5 模板×7 语言**（ZCode，1 天）：受限翻译档 7 语言模板（temperature=0+术语注入+`<translated>` 标记+数值/方向禁改），与 T2 L3 白话源文本对接。
- **回译 nightly + G1-G5 门禁**（Codex，0.5 天）：`05_backtranslate_check.py` 入 CI（nightly）；门禁：确定性 100%、术语命中 100%、回译 ≥98%、0 硬伤。
- **母语复核**（人工配合，用户安排）：`06_母语复核表.md`（53 术语×6 语言分层 + UI 76 条）交 6 语言复核人，返回后修订术语表。

### 验收
全 Board L0 查表（Astrolabe/Qizheng 会签除外）；BP1 键化落地并有快照测试；7 语言模板全；回译 nightly 运行 3 天无回归；母语复核 0 硬伤。

### 依赖 / 移交
- BP1 依赖 T1 会签 + T4 术语键格式；M1 与 T2 在 proxy.ts 同点位——**合并排期须主控协调**（两分支都改 proxy.ts）。
- 移交 T4：G1-G5 门禁并入 CI 建议。

### AI 地图查询 / 更新 / 交接（队 3 专用）
同队 1 三规范；关键词：i18n 多语言 术语 回译；产物只落 `thread-03-多语言翻译实现与准确性\`（**已从 git 分支恢复完整 20 文件**）；git 走 `thread/t3-i18n-accuracy`（已推，当前工作区 checkout 在此分支产物会「消失」，**产出后须回写主仓统一目录**——这是本队最大的交接断点教训，务必遵守）。

---

## 五、队 4 · T4 基础设施加固（剩余 部署/复测/第三片/套餐）

### 当前状态
- ✅ 根因（503=CF Error 1102 CPU 超限，泛端点随机 33%，21 体系静态导入）；修复 3 commit（98cad2a 500→400 / 34da981+165a81f 端点动态加载两片）；红线判定（2 fail 实为 g2 已修、partial 131 三态判定：3 pass/80 补证据含 10 P1/48 排期）；shared 三件套；回归 1539/108/229 全绿
- ❌ **未部署未验证**：线上 ziwei 实测 3 连测 1×200+2×1102，**503 仍在线发作**

### 目标
T4 修复上线，线上 0×5xx 验证通过；红线矩阵正式修订版发布；可选第三片懒加载评估。

### 剩余步骤
- **部署（主控执行，队 4 只备料）**：合并 `thread/t4-infra` → **bump sw v6→v7**（注意 T2 已占 v6）→ `wrangler pages deploy dist --project-name temposoul --branch main` → 线上验证 `repro_503.py` 3×100（ziwei/qimen/bazi 混合）**零 5xx**。
- **第三片懒加载评估**（Codex，可选 0.5 天）：bazi/calendar 静态链 + core/prompt barrel；若部署后仍有零星 1102 再执行。
- **CF 套餐确认**（人工配合，用户）：CF 控制台确认 per-request CPU 预算；若免费档 10ms，极端冷启动可能残留 1102 → 升付费档（30s）——需用户批示。
- **红线矩阵正式修订**（DSH+主控）：按 v2 建议版（3 吠陀 pass + 10 P1 必做 + 48 排期）终裁后更新 `468-redline-mapping.md`；gaps/mapping 陈旧行（1.2-96~99）刷新。
- **回滚预案**（随部署）：revert 165a81f/34da981 两 commit 即可回到原状，记录在 `03_修复记录.md`。

### 验收
线上 3×100 零 5xx（repro_503.py 实测数据入库）；500→400 线上复测通过；红线矩阵正式版发布；部署日志含回滚方案。

### 依赖 / 移交
- 无前置依赖（代码已备）；部署权在主控。
- 移交：门禁建议（含 T3 G1-G5）→ CI；CF 套餐结论 → 主控决策。

### AI 地图查询 / 更新 / 交接（队 4 专用）
同队 1 三规范；关键词：Worker 503 1102 部署 回滚；产物只落 `thread-04-基础设施加固与红线收口\` + `shared\`；git 走 `thread/t4-infra`；**部署动作仅主控可执行，队 4 不得自行 deploy**。

---

## 六、主控收口（豆包执行，最后一步）

1. **合并**：T1（ef0cb90 修复）、T2（thread/t2-translation-audit 剩余）、T3（thread/t3-i18n-accuracy）、T4（thread/t4-infra）→ `codex/website-basic-settings`；解决 proxy.ts 同点位冲突（T2/T3）。
2. **部署序列**：bump sw（当前 v6 → 视合并内容 v7）→ build → 全量三套测试（core/api/prompt）→ `wrangler pages deploy dist --branch main` → 线上验证（health/sw 版本/ziwei×3/repro_503 3×100）。
3. **红线终裁**：T2 2.1 申报 + T4 矩阵 v2 交用户确认后正式更新。
4. **人工配合项汇总**：CF 套餐、M2 verified 核验、母语复核 6 语言、T1 D 类专家裁决——列出清单交用户安排。
5. **记忆与 AI 地图**：按 6 步更新（部署日志/主地图/00_总览/更新日志/memory/write/corpus/scan）；`_更新日志.md` UTF-16LE 用 Python utf-16-le 追加。
6. **registry 补登记**：TempoSoul 网站组件表补登记（T3 发现）。

## 七、共用规范速查（每队必须遵守）

- **AI 地图查询**：三级漏斗（结构化索引→滚动上下文→8877 语义检索 k=5/8/3），检索后一句话汇报。
- **AI 地图更新**：里程碑 6 步；`_更新日志.md` 必须 Python utf-16-le 追加；敏感内容不写入。
- **交接**：产物唯一落点 + WORKLOG 实时写 + 状态标记 ✅/🔄/⚠️/❌ + 接手顺序 README→本计划书→WORKLOG→output。
- **红线**：禁止直接部署/合并 main/bump sw；禁止伪造（verified/比对数据/母语复核）；同一动作失败 2 次换通道并报告。
