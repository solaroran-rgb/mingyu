# T4-S6 · 回归报告（基线对比）

> 时间：2026-09-13 ｜ 执行者：ZCode ｜ 分支：`thread/t4-infra`（98cad2a → 34da981 → 165a81f）

## 结果

| 套件 | 基线（09-12 收口） | 本轮 | 差异 | 结论 |
|---|---|---|---|---|
| test:core | 1539 | **1539 全绿**（136s） | +0 | ✅ 不低于基线 |
| test:api | 107 | **108 全绿**（110–133s） | +1（T4 新增时区 400 回归） | ✅ |
| test:prompt | 229 | **229 全绿**（4–5s） | +0 | ✅ |

- 运行环境：Windows / node v22.23.2 / pnpm 11.9.0（corepack）；`CI=true` 防 pnpm TTY 坑。
- 新增测试：`tests/public-api.test.ts`「时区冲突与夏令时跳时应返回 400 而非 500」；`tests/civil-time.test.ts` 增 MingyuCoreError 类别/码断言。
- 既有测试零修改、零跳过。

## 门禁建议（CI 存在 GitHub Actions 时）

1. PR 门禁 = `test:core` + `test:api` + `test:prompt` 三套串行全绿（现 repo 无 CI 配置，属 S5 共享规范议题）。
2. 红线状态自动化（partial 判定 CI 化）暂缓——先由 T2/T3 结论稳定矩阵（见 05 §四）。

## 线上验证（须主控执行）

部署 `thread/t4-infra` 合并结果后：`python output/repro_503.py` 连续 ≥3 轮 × 100 请求 **0 个 5xx** 方可宣告 1102 闭环（任务卡验收第 1 条）。
