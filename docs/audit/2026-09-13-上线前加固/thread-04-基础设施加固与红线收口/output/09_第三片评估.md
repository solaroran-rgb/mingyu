# T4 · 第三片懒加载评估（只评估未实施，按主控指令挂起）

> 评估：ZCode @ 2026-09-13 ｜ 触发条件：部署后 `repro_503.py` 仍见零星 1102 才启动实施
> 数据源：esbuild metafile（`npx wrangler pages functions build --metafile`，740 inputs），分析脚本 `meta_analyze.py` 可重跑

## 一、当前静态/懒加载构成（源字节口径，总 6.65MB）

| 分组 | 体积 | 占比 | 状态 |
|---|---|---|---|
| 占卜组（liuyao/qimen/tarot/数据表等） | 1454 KB | 22% | ✅ 已懒加载（第二片） |
| bazi + calendar/ganzhi | 1024 KB | 15% | ⬜ 静态（第三片候选） |
| ziwei/iztro | 465 KB | 7% | ✅ 已懒加载（第二片） |
| 元学八体系 | 307 KB | 5% | ✅ 已懒加载（第一片） |
| location 数据 | 258 KB | 4% | ⬜ 静态（跟随 calendar） |
| prompt 文本 barrel | 187 KB | 3% | ⬜ 静态（第三片候选） |
| handler 自身 | 155 KB | 2% | ⬜ 静态（必须常驻） |
| 其它 node_modules/工具链 | 2796 KB | 42% | ⬜ 静态（多为共享依赖，含 tyme4ts 跟随 calendar） |

**结论：已懒加载 ≈ 2.23MB（33%，且是最重的计算模块 iztro+占卜数据）；第三片最多再收 ≈1.2–2MB（bazi/calendar 链 + 其依赖的 tyme4ts 大头在「其它」桶内跟随 calendar）。**

## 二、第三片收益/成本判断

- **收益**：冷启动再降 ~20–30% 静态求值量；bazi 端点（503 率 40%）受益最直接。
- **成本**：bazi 符号被 handler 约 15 个函数引用（计算/兼容/流年/prompt），async 级联面 ≈ 第二片的两倍；core/prompt barrel 由 4 个本地 re-exporter 静态引用，需一并改造或改子路径导入。
- **建议**：维持主控既定策略——**先部署验证**。判据：
  - 部署后 3×300 全 PASS → 第三片不做（收益边际，避免为 0 事故扩 diff）。
  - 仅 bazi/calendar 系端点残余 1102 → 值得做第三片（0.5 天，Codex）。
  - 全端点残余且 CF 为免费档 → 先升套餐（§5 runbook），第三片降级为可选。

## 三、实施要点（若启动，供接手者）

1. handler 的 `@temposoul/core/bazi`（3 处）+ `@core/bazi/baziCalculator`（src 别名）+ `/calendar`（7 符号）→ 各端点函数内 `await import`；baziCalculator 建议改从 `@temposoul/core/bazi` 子路径导入（消掉 src/dist 双副本来源，顺带简化 isMingyuCoreError 场景）。
2. prompt 四 re-exporter（astrolabe-prompts/metaphysics-prompt/divination/summary/astrolabe-synastry-prompt）改 `@temposoul/core/prompt/public-api` 等子路径或端点内动态加载。
3. 回归口径同 06_回归报告.md；tsc + test:api + test:prompt 必跑。
