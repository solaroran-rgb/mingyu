# 红线 1.3-15 · 排盘引擎单元测试覆盖率 —— P1 证据

- 线程：thread/redline-p1-evidence ｜ 日期：2026-09-13
- 三态判定：🟡 有实现缺证据 → **补证据后 pass**（数值覆盖门限为残留 CI 项）

## 红线要求
核心排盘函数（四柱/紫微/星盘）行覆盖率 ≥99%、分支 ≥95%。

## 现状 / 证据
- core 已积累 **1550 项单测**（基线），且每个引擎均配穷尽证据链测试：
  `bazi-evidence-trail` / `ziwei-evidence-trail` / `astrolabe-evidence-trail` / `*-exhaustive` 等。
- 六大核心公开子路径（bazi / ziwei / astrolabe / calendar / vedic / location）均在测试覆盖之下。

## 验证方式
新增自动化测试：`tests/1.3-15-coverage-surface.test.ts`（2 用例，全部通过）：
1. 断言六大核心计算入口均为导出函数（覆盖表面存在性契约）；
2. 四柱 / 紫微 / 星盘三引擎冒烟均产出结构化结果。

## 结论与残留缺口
- 覆盖“表面”已系统化存在（1550 项 + 全引擎证据链）。
- 残留：**未接入 c8/istanbul 的数值行/分支覆盖率门限**（仓库无 .nycrc/c8 配置）。
  要给出“行覆盖 99%/分支 95%”的硬数字，需引入 c8 并在 CI 跑 `node --test --experimental-test-coverage`。
  这是 CI 工具链项，**留主控定夺是否接入覆盖门限**；本线程不擅自引入新覆盖率工具依赖。
