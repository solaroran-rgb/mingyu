# 红线 1.3-10 · 南半球八字月令反转校验 —— P1 证据（**待主控裁决**）

- 线程：thread/redline-p1-evidence ｜ 日期：2026-09-13
- 三态判定：**❌ 经本线程核查确认 core 无实现 → 待主控裁决**（不自行实现、不伪造测试）

## 红线要求
提供“按节气（北半球逻辑）”与“对冲月令（南半球逻辑）”两种口径，南半球出生者月令可反转。

## 本线程核查结论
对 `packages/core/src` 全量检索 `南半球 / southern / hemisphere / 月令反转` 等关键词：
- **未发现任何南半球分支或月令反转开关**。
- 月柱由 `baziCalculator` 依据（真太阳时校正后的）节气推导，默认走**北半球节气逻辑**。
- 此与 `468-redline-gaps.md` 原文一致：“南半球月令反转选项在 core 未发现显式分支；默认北半球节气逻辑”。

## 为何不自行实现 / 不加测试
1. 这是**核心排盘算法的行为变更**（月柱、节气司权、十神旺衰全链依赖月令），回归面大，
   会冲击 core 1550 既有金标准；
2. 接口设计（开关名、默认值、南半球判定阈值、是否影响紫微/星盘）属产品法理决策；
3. 本线程约束“禁止实现待主控裁决项 / 不得伪造测试”。
因此**未新增对应自动化用例**（无实现可测）。

## 建议（交主控裁决）
- 是否立项：新增 `hemisphere: 'north' | 'south'`（或按纬度自动判定）入参，
  `south` 时月柱对冲、节气司权按反半球处理；
- 若立项，需配套金标准用例（南半球代表出生：如悉尼 1 月 vs 北京 1 月月令相反）。

## 现状
排盘默认北半球逻辑可正常工作；南半球月令反转 = **缺失功能，待裁决**。
