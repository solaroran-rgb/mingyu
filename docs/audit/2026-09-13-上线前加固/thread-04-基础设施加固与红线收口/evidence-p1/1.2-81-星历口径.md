# 红线 1.2-81 · 行星黄经星历口径 —— P1 证据

- 线程：thread/redline-p1-evidence ｜ 日期：2026-09-13
- 三态判定：🟡 有实现缺证据 → **补证据后 pass**（星历选择口径已文档化）

## 红线要求
行星黄经计算所用星历版本应文档化、可对质，结果确定性可复算。

## 实现位置 / 入口
- `packages/core/src/divination/algorithms/astrolabe.ts`（`generateAstrolabe`）
- 星历后端：**astronomy-engine 2.1.19**（VSOP/ELP 解析级数 + Meeus；见 `packages/core/package.json` 钉版）。
- 对质用例：`tests/meeus-golden.test.ts`、`tests/astrolabe-native-reference.test.ts`。

## 重要口径说明（与“Swiss Ephemeris”的差异）
本项目**未接入 Swiss Ephemeris（SWISS.EPH 二进制星历）**，而是采用 astronomy-engine 2.1.19 解析星历。
gaps 原文已确认此点，并建议“文档化 astronomy-engine 星历版本与 Swiss Ephemeris 对质口径”。
本证据即履行该文档化职责；与 Swiss Ephemeris 的残差量级（亚角秒级，对占星宫位判定无影响）见 astrolabe-native-reference 对质。

## 验证方式
新增自动化测试：`tests/1.2-81-ephemeris.test.ts`（3 用例，全部通过）：
1. 黄金值：2024-06-21 12:00 北京（UTC+8）太阳黄经 ∈ (90.0, 90.6)°（实测 90.29°，夏至刚入巨蟹）；
2. 全部行星黄经有限且归一到 [0,360)；
3. 同一输入两次排盘行星黄经完全一致（确定性，temperature 无关）。

## 结论与残留缺口
- 星历版本已钉版（2.1.19）、输出确定性与黄金值已回归。
- 残留：是否正式迁移到 Swiss Ephemeris 二进制星历，为工程选型决策，**留主控定夺**；当前口径下残差不影响排盘。
