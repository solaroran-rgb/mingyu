# BP1 引擎键化 · 会签材料与会签结论（T3 队 3 留档）

> 2026-09-13 ｜ 起草：ZCode（T3）｜ 会签状态：**待 T1 签署**（T4 术语键格式 v1 已为既有前提）
> 依据：队 3 任务指令「AstrolabeBoard/QizhengBoard 有意不接（同名不同义，正解 = BP1 引擎键化），记录会签结论」+「BP1 引擎键化（待 T1 键化会签）」

## 一、已定会签结论 ①：前端 shim 的语境边界（本线程结论，即刻生效）

| Board | 决策 | 理由 |
|---|---|---|
| BaziChartBoard / ZiweiTraditionalBoard / ChartStar / ZiweiBoard | ✅ 已接 zh→key→locale shim | 渲染语境与紫微/八字术语域一致 |
| AstrolabeBoard（西洋占星） | ❌ **不接**，待 BP1 | zh 标签「太阳/月亮/上升」在西洋语境是 planet/angle（astrology:planet:sun），与紫微星曜 ziwei:star:taiyang 同名不同义；盲套 shim 必然语境错译 |
| QizhengBoard（七政四余） | ❌ **不接**，待 BP1 | 同理：七政的「太阳/太阴」是天体而非紫微星曜，需 astrology 域键 |

**替代路径**：BP1 落地前，上述两板保持中文原文（诚实展示）而非错译；并行路径可扩术语表 astrology 域（en/es 名为 Sun/Moon/Ascendant 等已确立词，风险低），属 tier2 填充，不阻塞 BP1。

## 二、待 T1 会签结论 ②：BP1 引擎输出键化范围（本线程提案，待签）

**提案范围**（第一批，只做排盘 L0 高频实体，不动文案层）：
1. `bazi:stem:*`（10）`bazi:branch:*`（12）`bazi:zodiac:*`（12）`bazi:wuxing:*`（5）——`packages/core/src/ganzhi/data.ts` 常量对象化：`HEAVENLY_STEMS: { key, zh }[]`（保留原数组兼容导出，防 1539 core 测试雪崩）。
2. `bazi:shishen:*`（10）——十神枚举处。
3. `ziwei:star:*` / `ziwei:palace:*` / `ziwei:sihua:*`——iztro 输出映射层（`build-analysis-payload/helpers/builders.ts`）加 key 字段。
4. `yijing:gua:*`（8+64）——卦名输出结构。
5. 干支组合（60 甲子）`bazi:jiazi:*`——组合处按位拼 key。

**会签要点（请 T1 确认）**：
- [ ] 键名以 `terms/7lang-terms.csv` 现行 355 键为基准（已对齐 T4 v1 三段式），slug 为 pinyin 连写——T1 审计中若发现引擎侧命名惯例冲突，以四线程会签为准；
- [ ] 输出结构加键采用「附加字段」（`stem_key` / `palace_key` 或统一 `archetype_key`），不破坏既有字段（旧消费者零感知）；
- [ ] 快照机制：键表变更必须重跑 `scripts/i18n/gen-terms-7lang.py` 并更新 `shared/cases/_keys_snapshot.json`（CI i18n-nightly 已有防漂移检查）。

**测试保护现状**：`tests/terms-7lang.test.ts` 锁定键格式/唯一性/规模；BP1 落地时同步扩展为引擎输出断言（G1 完整版）。

## 三、会签记录栏

| 线程 | 结论 | 日期 | 签署 |
|---|---|---|---|
| T3 | 提案 + shim 边界结论（上表） | 2026-09-13 | ZCode ✅ |
| T1 | **✅ 同意提案范围第一批（5 项全签）**，附 4 项条件（见下） | 2026-09-13 | ZCode（T1 执行席）✅ |
| T2 | （已知悉：T2 词库锚定共用键，见主地图 09-13 记录） | — | — |
| T4 | 键格式 v1 既有前提（shared/术语键格式.md） | 2026-09-13 | T4 既有 ✅ |

## 四、T1 会签意见（2026-09-13，依据 thread-01 规则审计证据）

1. **范围 ✅**：五项提案与 T1 审计证据完全兼容——①ganzhi/data.ts 是单一真相源（foundation-toolkit 测试断言 bazi 旧表与地基层同一对象引用），常量对象化+保留原数组兼容导出正确；②十神为本地 10×10 矩阵（baziUtils.ts:107-127，ten-gods-assertions 锁定），加键点明确；③iztro@2.5.8 为 CJS vendor 整包（不可 tree-shake），**只在 build-analysis-payload 映射层加键不碰 vendor 是唯一正确姿势**；④64 卦/⑤60 甲子在术语表均已 7 语言全填（64/64、60/60），风险最低。
2. **命名基准 ✅**：以 terms/7lang-terms.csv 现行 355 键 + T4 v1 三段式为准。T1 审计确认全仓 `archetype_key` 既有消费为 0（T2 证据），无引擎侧命名惯例冲突；**键即契约：core 侧只产 key 不产译文**。
3. **附加字段式 ✅**：落地时必须跑 core/api/prompt 全量回归（T1 分支口径 core 1538/1540·api 108·prompt 229），并在 G1 完整版加「旧字段与 key 字段一致性」断言——防 key 与 zh 脱钩成第二张表（T1 的 H1 双份表教训移植：键生成只走 gen-terms-7lang.py 单一来源 + 快照 CI）。
4. **边界维持 ✅**：Astrolabe/Qizheng 有意不接的结论与 T1 审计 B9 类证据一致（太阳=planet vs 紫微太阳星曜，同名不同义）；astrology 域键走 tier2 填充，不阻塞 BP1。
5. **口径提示**：bazi:zodiac:* 键语义=年支生肖（立春界年支，B1 口径），与流年干支键独立；60 甲子拼键按 pinyin 连写规范（T3 已定）。
