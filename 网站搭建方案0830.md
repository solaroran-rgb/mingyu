# 🌌 TempoSoul · 命律 — 全站一体化部署总方案

> **项目名称**：TempoSoul · 命律 | **版本**：v1.0 Ultimate | **日期**：2026-08-30  
> **定位**：AI 驱动的东方命理与生命指引平台 | **技术栈**：Cloudflare Pages + React 19 + Vite 7 + pnpm Monorepo

---

## 〇、方案导航与全局架构

### 0.1 四大专家责任域总览

| 专家    | 姓名  | 责任域                            | 覆盖需求             | 产出文件量  |
| ----- | --- | ------------------------------ | ---------------- | ------ |
| **A** | 陈天衡 | 计算引擎 · API 契约 · 测试 · 质量红线      | §3.1 §3.2 §3.8   | ~24 项  |
| **B** | 林星澜 | 前端应用 · PWA · 信任引擎 · i18n · SEO | §3.3 §3.4 §3.5   | ~120 项 |
| **C** | 苏文枢 | 词库 · 内容多语言 · AI 管线 · 合规文案      | §3.6 §3.7        | ~30+ 项 |
| **D** | 岳镇山 | DevOps · 认证支付 · 安全合规 · CI/CD   | §3.9 §3.10 §3.11 | ~10 项  |

### 0.2 核心技术决策一览

| 决策项         | 选择                                                                 | 理由                    |
| ----------- | ------------------------------------------------------------------ | --------------------- |
| **引擎**      | temposoul-core v0.3.0（唯一）                                          | 消除多实现混乱，单一事实源         |
| **响应包络**    | `{ok, data\|error{code,message}, meta{service,version}}`           | 全 62 端点统一，前后端契约基石     |
| **证据型输出**   | `calculationSteps` / `evidence` / `limitation` / `counterEvidence` | 核心壁垒——"证据给你，判断留你"     |
| **状态管理**    | Zustand                                                            | 轻量、无 Provider 嵌套、精准订阅 |
| **样式方案**    | CSS Modules + Design Tokens                                        | 零运行时开销，全局变量统一         |
| **渲染策略**    | SPA（初期）→ 评估预渲染                                                     | 成本铁律，流量增长后升级          |
| **部署平台**    | Cloudflare Pages + Workers                                         | 全球边缘、零服务器、成本可控        |
| **支付网关**    | Stripe（KV 幂等）                                                      | 出海市场标配，KV 防重复发放       |
| **Node 版本** | ≥ 22.0.0（全链路强制）                                                    | 消除本地/容器版本鸿沟           |
| **包管理**     | pnpm ≥ 11.9.0 + corepack                                           | 确定性构建，版本锁定            |

### 0.3 跨专家依赖拓扑图

```
┌─────────────────────────────────────────────────────────┐
│                    M0 引擎基线门                          │
│  A: git止血 → 证据契约 → 测试迁移 → 红线自动化 → v0.3.0  │
└────────────┬────────────────────────────────────────────┘
             │ v0.3.0 npm 包 + 62端点契约 + 黄金用例
    ┌────────┼────────┬──────────────┐
    ▼        ▼        ▼              ▼
┌───────┐ ┌───────┐ ┌───────┐  ┌──────────┐
│  B    │ │  C    │ │  D    │  │  B+C     │
│前端    │ │词库    │ │部署    │  │信任引擎   │
│工程    │ │AI管线  │ │认证    │  │T0-T6     │
│基线    │ │内容    │ │支付    │  │联调      │
│M1     │ │M2     │ │M3/M4  │  │M5        │
└───┬───┘ └───┬───┘ └───┬───┘  └─────┬────┘
    │         │         │            │
    └─────────┴─────────┴────────────┘
              │
              ▼
       ┌─────────────┐
       │ M6 全站上线   │
       │ 集成验收门    │
       └─────────────┘
```

---

## 一、里程碑总控与验收门（Master Milestone Plan）

### 1.1 里程碑全景

| 里程碑      | 名称                  | 负责人 | 前置依赖     | 核心交付物                               | 预估工期    |
| -------- | ------------------- | --- | -------- | ----------------------------------- | ------- |
| **M0.1** | 双仓 git 止血 + 改名收口    | A+D | 无        | git status 清零；mingyu→temposoul 全量替换 | 2 天     |
| **M0.2** | 引擎证据契约 + 导出强化       | A   | M0.1     | 四大证据字段类型定义；exports 子路径全量映射          | 3 天     |
| **M0.3** | 测试体系迁移归一            | A   | M0.2     | 120+112 测试全绿；契约测试锁定                 | 3 天     |
| **M0.4** | 质量红线 A 侧自动化         | A   | M0.3     | gates-A.json；CI 阻断门挂载               | 2 天     |
| **M0**   | ⭐ **引擎 v0.3.0 基线门** | A   | M0.1~0.4 | npm 包发布；tag/包/meta 三处版本一致           | **验收日** |
| **M1**   | 前端工程基线 + API 全量强化   | A+B | M0       | 62 端点核对单全勾；前端骨架+路由+状态管理             | 5 天     |
| **M2**   | 词库数据 + 内容管线就绪       | C   | M0       | archetype_key 枚举清单；七语种语言包；合规文案      | 5 天     |
| **M3**   | 认证 + 支付 + 安全部署      | D   | M1       | AUTH_ENABLED 可用；Stripe 沙箱跑通；CSP A-级 | 3 天     |
| **M4**   | 信任引擎 T0-T6 全链路联调    | B+C | M1+M2    | T0 星空→T6 付费墙全流程跑通                   | 5 天     |
| **M5**   | i18n + SEO + 无障碍收口  | B   | M4       | 七语种切换无裸露；Lighthouse ≥ 90            | 3 天     |
| **M6**   | ⭐ **全站集成验收门**       | ALL | M0~M5    | 黄金用例端到端通过；SLO 达标                    | **上线日** |

### 1.2 验收门硬指标

| 验收门      | 必须满足的条件（全部 PASS 才过）                                                                                                                                                                    |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **M0 门** | ① git status 干净 ② mingyu 活代码搜索=0 ③ 四证据字段全体系接入 ④ Meeus 黄金集全绿 ⑤ 120+112 测试全绿 ⑥ gates-A 聚合器 exitCode 正确 ⑦ npm tag v0.3.0 发布 ⑧ 线上 aov.cc 与本地同输入同输出                                         |
| **M6 门** | ① 62 端点本地 wrangler dev 全可调通 ② 前端 Lighthouse Performance≥90 / LCP<2.5s / CLS<0.1 ③ 七语种切换无键名裸露 ④ 键盘可完成全部核心流程（WCAG 2.1 AA） ⑤ CSP 扫描 A- 级以上 ⑥ SLO: P95<800ms / 错误率<1% ⑦ 全流程无恐吓性文案 + 免责声明常驻 |

---

## 二、阶段一：工程止血与改名收口（M0.1）

### 2.1 引擎仓 185 项未提交变更处理

| 步骤      | 产出文件                                                                | 动作要点                                         | 验收标准                                          |
| ------- | ------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------- |
| 变更分类审计  | `temposoul-core/docs/git-hygiene-2026-08.md`                        | 185 项按主题分 6~10 批：①真太阳时 ②占卜算法 ③测试 ④文档 ⑤依赖 ⑥杂项 | 引擎仓 `git status` 清零；185 项与批次总数对账一致            |
| 拆分为特性分支 | `feat/true-solar-opt` / `feat/new-divination` / `chore/test-update` | 不合入 v0.3.0 基线，保留决策灵活性                        | `git branch -r --list 'origin/feat/*'` 可见三个分支 |
| 全量测试基线  | `temposoul-core/docs/test-baseline-2026-08.md`                      | 记录 112 个测试文件的通过/跳过/失败及原因分类                   | 基线跑完后无"未知失败"                                  |

### 2.2 主仓 29 项未提交变更处理

| 步骤          | 动作要点                                | 验收标准               |
| ----------- | ----------------------------------- | ------------------ |
| 特性分支合回 main | `codex/website-basic-settings` 合回主干 | 主仓 `git status` 干净 |
| 逐文件审查       | 29 项逐一审查，不合格的重写                     | 无遗留低质量代码           |

### 2.3 全局改名收口铁律（mingyu → temposoul）

| 收口位置                              | 当前状态                   | 收口动作                          |
| --------------------------------- | ---------------------- | ----------------------------- |
| `package.json` build 脚本           | `--filter @temposoul/core` | 改为 `--filter temposoul-core`  |
| `docker-compose.yml` 服务名          | `mingyu`               | 改为 `temposoul`                |
| `.well-known/aov-mingyu-api.json` | 旧文件名                   | 重命名为 `aov-temposoul-api.json` |
| `manifest.webmanifest` name       | 可能含旧名                  | 确保为 `TempoSoul · 命律`          |
| `index.html` title/meta           | 可能含旧品牌名                | 统一为 `TempoSoul · 命律`          |
| 远程仓名                              | `Brhiza/mingyu.git`    | GitHub rename 为 `temposoul`   |
| 所有 `src/` 文件                      | 全局搜索 `mingyu`          | 逐一替换为 `temposoul`             |

> **验收命令**：`grep -ri "mingyu" src/ public/ index.html package.json --exclude-dir=node_modules --exclude-dir=.git` 返回零结果

---

## 三、阶段二：引擎证据契约与导出强化（M0.2）

### 3.1 四大证据字段类型定义

在 `temposoul-core/src/types/evidence.ts` 中定义全引擎统一的证据结构：

| 字段                 | 类型                                                         | 用途                   | 红线要求             |
| ------------------ | ---------------------------------------------------------- | -------------------- | ---------------- |
| `calculationSteps` | `Array<{step, name, formula?, input, output, reference?}>` | 计算链——每步含公式与出处        | 覆盖全部关键换算步骤       |
| `evidence`         | `Array<{claim, source, sourceType, locator?}>`             | 结论出处，sourceType 枚举四类 | 核心断言必须标注         |
| `limitation`       | `Array<{scope, description}>`                              | 解释边界                 | ≥1 条/体系          |
| `counterEvidence`  | `Array<{condition, alternative}>`                          | 反证/例外                | ≥1 条强制，红线目标 ≥4 条 |

**分批接入优先级**：

- 🥇 第一批：八字 + 紫微 + 真太阳时（MVP 核心）
- 🥈 第二批：六爻 / 梅花 / 奇门 / 大六壬 / 小六壬
- 🥉 第三批：塔罗 / 雷诺曼 / 灵签 / 黄历 / 西洋星盘
- 🏅 第四批：风水三体系 + metaphysics 其余

### 3.2 引擎导出契约（exports 子路径）

```
temposoul-core/
├── /bazi                    # 八字
├── /ziwei/default           # 紫微（默认安星法）
├── /ziwei/zhongzhou         # 紫微（中州派安星法）
├── /divination/liuyao       # 六爻
├── /divination/qimen        # 奇门
├── /divination/tarot        # 塔罗（含 15 牌阵枚举）
├── /divination/lenormand    # 雷诺曼
├── /metaphysics/bazhai      # 八宅
├── /metaphysics/xuankong    # 玄空
├── /metaphysics/qizheng     # 七政四余
├── /calendar/true-solar-time
├── /calendar/moon-phase
├── /calendar/solar-term
├── /foundation/ganzhi
├── /foundation/wuxing
├── /foundation/shensha
├── /types/evidence
└── /prompt                  # 提示词生成器族
```

> **验收**：逐一 import 每个子路径成功；tree-shaking 后 bazi 单体系 < 300KB

### 3.3 紫微双轨依赖隔离

| 动作                                             | 说明                         |
| ---------------------------------------------- | -------------------------- |
| `iztro` 与 `celestine` 移至 `peerDependencies`    | 标记 `optional: true`，避免打包冲突 |
| 新增 `./ziwei/default` 与 `./ziwei/zhongzhou` 子路径 | 分别引入不同安星法                  |
| 引擎包体积目标                                        | 比 v0.2.0 减少约 15%           |

### 3.4 真太阳时精度验收（核心壁垒）

**Meeus 第 28 章黄金数据集**覆盖维度：

- 全年 24 节气点 + 100 个随机时刻均时差（允差 ≤ 1 秒）
- 经度 −180°~+180° 每 15° 采样
- IANA 历史时区边界（1949 前后中国时区、夏令时切换日）
- 南半球契约用例（悉尼、布宜诺斯艾利斯）
- 子时双口径用例（晚 23:00–24:00 vs 00:00–01:00）
- **线上实测锚点**：北京 116.4074°、1990-05-15 10:30:20 → −10.6155 分

---

## 四、阶段三：测试迁移与质量红线（M0.3 ~ M0.4）

### 4.1 测试迁移映射（120 + 112 文件）

| 迁移动作      | 含义                                   | 处理策略                |
| --------- | ------------------------------------ | ------------------- |
| **R** 重定向 | 改 import 到 temposoul-core 对应子路径，断言不变 | 优先执行                |
| **D** 删除  | 测的是已废弃能力                             | 确认后删除               |
| **W** 重写  | 旧引擎行为与新引擎有差异                         | 记录差异原因，附 Meeus/古籍依据 |

**契约测试层**：锁定 "API 返回 = 引擎直接调用返回" 的等价性，黄金用例覆盖每体系典型 + 边界（1900/2100 年、timezone ±12/14、participants=30）。

### 4.2 质量红线分级执行表

| 级别                | 判定标准            | 代表条目                                                   | CI 行为              |
| ----------------- | --------------- | ------------------------------------------------------ | ------------------ |
| 🔴 **阻断 Blocker** | 破坏即"算错/崩溃/契约破裂" | 真太阳时偏差>1s；子时双口径错误；南半球契约失效；响应包络缺 meta；zod 校验缺失；黄金用例漂移   | **build 不执行**      |
| 🟡 **警告 Warning** | 破坏不致命但降质量       | counterEvidence<4 条；calculationSteps 缺 formula；测试跳过未豁免 | build 通过 + PR 评论标注 |

**CI 顺序**：`lint → type-check → 单测(120) → quality-gates 聚合器(阻断门) → build`

---

## 五、阶段四：API 全量强化（M1）

### 5.1 API 端点族文件级落点

**目录结构**：

```
functions/api/v1/
├── .ts              # catch-all 路由分发
├── routes/registry.ts       # 端点注册表（路径→zod schema→handler 映射）
├── _middleware.ts            # 全局中间件（CORS/限流/requestId）
├── middleware/
│   └── responseEnvelope.ts  # 统一响应包络 + X-RateLimit-* 透传
└── schemas/
    ├── common.ts            # 经纬度/时间索引/紫微流派/15种牌阵枚举
    ├── calendar.schema.ts   # calendar 族（5 端点）
    ├── foundation.schema.ts # foundation 族（4 端点）
    ├── bazi.schema.ts       # bazi/ziwei 族（9+1 端点）
    ├── divination.schema.ts # divination 族（11×2 端点）
    ├── metaphysics.schema.ts# metaphysics 族（8×2 端点）
    └── ai.schema.ts         # ai/系统族
```

### 5.2 关键 API 契约规范

**统一 Zod 校验枚举**：

| 校验项            | 约束                         |
| -------------- | -------------------------- |
| `year`         | `[1900, 2100]`             |
| `longitude`    | `[-180, 180]`              |
| `latitude`     | `[-90, 90]`                |
| `timezone`     | `[-12, +14]`               |
| `timeIndex`    | `['0'...'11']`（子时到亥时）      |
| `participants` | `min(2).max(30)`           |
| `ziwei school` | `['default', 'zhongzhou']` |
| `tarot spread` | 15 种枚举值完整覆盖                |

**合盘端点入参结构**：

```
{ participants: [{gender, year, month, day, timeIndex, calendarType, longitude, latitude}, ...],
  analysisType: 'bazi' | 'ziwei' | 'both' }
```

**跨切面中间件**：

| 中间件   | 功能                                          | 交接点                             |
| ----- | ------------------------------------------- | ------------------------------- |
| CORS  | 允许 aov.cc 同源 + 白名单域                         | D 配置 `CORS_ALLOWED_ORIGINS`     |
| 限流预留  | 注册表限流档位字段，M3 前全部 `unlimited`                | D 提供 `rate-limit.ts` 实现         |
| 错误追踪  | 5xx 响应附 `meta.requestId`（crypto.randomUUID） | B 的 ErrorBoundary 上报时携带         |
| 运行时配置 | `/temposoul-runtime-config.js` 动态注入环境变量     | Key 永不注入；`AUTH_ENABLED` 使前端优雅降级 |

### 5.3 MCP Server 工具注册

- 每体系独立 tool（非聚合），约 40 个工具
- 复用 API 层 zod schema（单一事实源）
- 出参透传证据字段——外部 Agent 天然获得"证据型输出"
- 验收：`tools/list` 返回数组长度 > 20，含 `tarot_grandTableau`

---

## 六、阶段五：前端应用全量建设（M1 ~ M5）

### 6.1 工程基线

| 配置项            | 规格                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------- |
| **TypeScript** | `strict: true`；`moduleResolution: bundler`；`verbatimModuleSyntax: true`                     |
| **Vite 构建**    | `outDir: dist`；`sourcemap: hidden`；manualChunks 拆分 vendor-react / engine-core / vendor-i18n |
| **ESLint**     | flat config + typescript-eslint + react-hooks + **jsx-a11y**（无障碍规则）                         |
| **代码分割**       | 首屏 InputPage 不懒加载；其余路由 `React.lazy + Suspense`                                              |
| **性能预算**       | 主入口 chunk < 200KB(gzip)；总 CSS < 50KB(gzip)；字体 < 100KB(gzip)                                 |

### 6.2 全局状态管理（Zustand）

| Store         | 核心字段                                                                  | 持久化          |
| ------------- | --------------------------------------------------------------------- | ------------ |
| `appStore`    | currentSystem, theme(dark), isOnline, pwaInstallPrompt, engineVersion | —            |
| `resultStore` | currentResult(含完整证据字段), resultHistory, isLoading, error               | IndexedDB    |
| `authStore`   | isAuthenticated, user, sessionToken（AUTH_ENABLED=false 时全 null）       | —            |
| `i18nStore`   | locale, direction(ltr\|rtl), fallbackChain                            | localStorage |

### 6.3 路由与页面体系

| 路径                   | 组件                       | 懒加载 | 核心职责                                 |
| -------------------- | ------------------------ | --- | ------------------------------------ |
| `/`                  | InputPage                | ❌   | 排盘输入 + T0 星空 + T1 今日宇宙               |
| `/result/:id`        | ResultPage               | ✅   | 图表渲染 + 解读 + T4 证据面板 + T5 叙事 + T6 付费墙 |
| `/records`           | RecordsPage              | ✅   | 历史记录（本地 IndexedDB，认证降级提示）            |
| `/tutorial`          | TutorialPage             | ✅   | 命理科普教程（专家 C 文案）                      |
| `/lexicon`           | LexiconPage              | ✅   | 777 条术语虚拟滚动搜索                        |
| `/lexicon/:key`      | LexiconDetailPage        | ✅   | 术语详情 + 出处 + 关联                       |
| `/login` `/register` | LoginPage / RegisterPage | ✅   | AUTH_ENABLED=false 时表单禁用 + 降级提示      |
| `/privacy`           | PrivacyPage              | ✅   | 隐私政策 + GDPR 数据删除入口                   |
| `/subscription`      | SubscriptionPage         | ✅   | 订阅方案 + Stripe Checkout               |

### 6.4 多体系表单动态字段矩阵

| 字段                     | 八字  | 紫微  | 六爻  | 塔罗  | 西洋星盘 | 风水    |
| ---------------------- | --- | --- | --- | --- | ---- | ----- |
| 姓名（可选）                 | ✅   | ✅   | ❌   | ❌   | ✅    | ❌     |
| 性别                     | ✅   | ✅   | ❌   | ❌   | ✅    | ❌     |
| 出生日期/时辰                | ✅   | ✅   | ❌   | ❌   | ✅    | ❌     |
| 出生地（经纬度）               | ✅   | ✅   | ❌   | ❌   | ✅    | ✅(地址) |
| 真太阳时开关                 | ✅   | ✅   | ❌   | ❌   | ✅    | ❌     |
| 问题文本                   | ❌   | ❌   | ✅   | ✅   | ❌    | ❌     |
| 牌阵选择（15 种）             | ❌   | ❌   | ❌   | ✅   | ❌    | ❌     |
| 安星法（default/zhongzhou） | ❌   | ✅   | ❌   | ❌   | ❌    | ❌     |
| 合盘模式                   | ✅可选 | ✅可选 | ❌   | ❌   | ✅可选  | ❌     |

### 6.5 多体系图表渲染方案

| 组件                  | 渲染技术              | 核心特性                       |
| ------------------- | ----------------- | -------------------------- |
| **BaziChart**       | HTML/CSS          | 四柱横排 + 十神/藏干/纳音/神煞 + 大运时间轴 |
| **ZiweiChart**      | HTML/CSS          | 十二宫格 4×4 + 庙旺利陷颜色 + 四化标记   |
| **AstrolabeChart**  | **SVG**（非 Canvas） | 矢量相位线 + DOM 事件交互 + 合盘双盘叠加  |
| **HexagramChart**   | HTML/CSS          | 六爻爻象 + 变爻红色标记 + 六亲/六神      |
| **TarotSpread**     | HTML/CSS          | 15 种牌阵预定义 + 翻牌 rotateY 动画  |
| **FengshuiCompass** | SVG               | 二十四山同心圆 + 九宫格飞星盘           |

### 6.6 信任引擎 T0–T6 全链路

| 层级     | 时间段      | 核心组件                                                      | 转化目标              | 埋点事件                                              |
| ------ | -------- | --------------------------------------------------------- | ----------------- | ------------------------------------------------- |
| **T0** | 0–5s     | StarfieldBackground（Canvas 粒子星空 + 视差）                     | 视觉震撼 + 品牌认知       | `starfield_rendered`                              |
| **T1** | 5–20s    | TodayCosmos（月相/节气/干支三卡片）+ PrecisionDemo + DailyReading    | 零输入即获价值           | `today_cosmos_viewed` / `precision_demo_expanded` |
| **T2** | 20–60s   | PrivacyShield + LocalComputeBadge + PrivacyHint           | 隐私安全信任            | `privacy_hint_shown` / `form_started`             |
| **T3** | 60s–3min | RitualLoader（仪式感加载 + 公式展示）+ HookSentence + PillarMetaphor | 排盘仪式感 + 首屏钩子句     | `form_submitted` / `ritual_loader_completed`      |
| **T4** | 3–10min  | EvidencePanel（计算链/古籍出处/解释边界/反证 四标签页）                      | 法理穿透——"证据给你，判断留你" | `evidence_panel_opened` / `evidence_step_clicked` |
| **T5** | 10min+   | ThreeLivesNarrative（三世叙事卷轴）+ BlindTestWidget              | 深度沉浸 + 趣味互动       | `three_lives_started` / `three_lives_completed`   |
| **T6** | 转化       | PaywallGate（模糊遮罩）+ UpgradeCard + ConversionTracker        | 付费转化——"贩卖已知的从容"   | `paywall_viewed` / `checkout_clicked`             |

**信任引擎全局验收**：

- ✅ 全流程无恐吓性文案
- ✅ 免责声明常驻
- ✅ 危机干预入口可见
- ✅ `ANALYTICS_PROVIDER=none` 时无第三方请求

### 6.7 PWA 基础设施

| 组件                        | 缓存策略             | 说明                    |
| ------------------------- | ---------------- | --------------------- |
| 静态资源（JS/CSS/图片/字体）        | **CacheFirst**   | maxEntries=100，30 天过期 |
| GET /api/v1/（只读端点）        | **NetworkFirst** | 5s 超时回退缓存             |
| POST /api/v1/（排盘请求）       | **NetworkOnly**  | 不缓存，结果由前端写入 IndexedDB |
| 导航请求（HTML）                | **NetworkFirst** | 离线回退 offline.html     |
| manifest / runtime-config | **NetworkFirst** | 3s 超时，确保配置及时生效        |

### 6.8 七语种 i18n 架构

| 语种        | locale | 方向      | 特殊处理                                  |
| --------- | ------ | ------- | ------------------------------------- |
| English   | en     | ltr     | 默认回退语言                                |
| Español   | es     | ltr     | —                                     |
| العربية   | ar     | **rtl** | 布局镜像 + 东阿拉伯数字 + letter-spacing:0      |
| ไทย       | th     | ltr     | line-height: 2.0（上下标元音空间）             |
| Português | pt     | ltr     | —                                     |
| 中文        | zh-CN  | ltr     | word-break: keep-all；line-height: 1.8 |
| 日本語       | ja     | ltr     | 同 CJK 排版                              |

**回退链**：当前语言 → en → zh-CN  
**持久化**：localStorage 键 `temposoul-locale`  
**SEO 交叉**：`og:locale` + `hreflang` 七语种互引

### 6.9 性能预算表

| 指标        | 预算             | 实现手段                                                    |
| --------- | -------------- | ------------------------------------------------------- |
| **LCP**   | < 2.5s         | 关键 CSS 内联；首屏图片 preload；星空 Canvas 异步初始化                  |
| **INP**   | < 200ms        | startTransition；虚拟滚动；useMemo / useCallback              |
| **CLS**   | < 0.1          | 图片声明 width/height；font-display: swap；星空 position: fixed |
| **TTFB**  | < 800ms        | CF 边缘节点（D 配置）                                           |
| **首屏 JS** | < 200KB (gzip) | 代码分割；manualChunks；tree-shaking                          |
| **总 CSS** | < 50KB (gzip)  | CSS Modules；tokens.css 变量                               |

---

## 七、阶段六：认证、支付与商业化部署（M3）

### 7.1 认证体系

| 组件              | 实现                                | 说明                                |
| --------------- | --------------------------------- | --------------------------------- |
| **AUTH_KV**     | CF KV 命名空间                        | 会话 Token 存储 + 登出黑名单（TTL 24h）      |
| **AUTH_SECRET** | `wrangler secret put` 注入          | `openssl rand -hex 32`，64 字符强随机密钥 |
| **JWT**         | payload: userId(脱敏哈希) + exp + iat | 边缘认证中间件校验                         |
| **优雅降级**        | `AUTH_ENABLED=false` 时            | 前端表单禁用 + 提示条"账户功能将在正式部署后启用"       |

### 7.2 Stripe 支付集成

| 设计要点           | 实现                                                                   |
| -------------- | -------------------------------------------------------------------- |
| **Webhook 幂等** | KV Key: `idempotency:stripe:{event.id}`，TTL 24h；重复事件直接返回 200         |
| **签名验证**       | `stripe.webhooks.constructEvent` 验证 `stripe-signature` header        |
| **多币种映射**      | `config/stripe-products.json`：业务权益 ↔ Stripe Price ID，支持 USD/EUR 动态切换 |
| **Checkout**   | `POST /api/v1/billing/create-checkout-session` → 跳转 Stripe Checkout  |

### 7.3 安全合规

| 安全措施                       | 配置                                                                                                                         |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **HSTS**                   | `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`                                                  |
| **CSP**                    | `script-src 'self'`；`connect-src 'self' https://api.deepseek.com https://api.stripe.com`；`frame-src https://js.stripe.com` |
| **X-Content-Type-Options** | `nosniff`                                                                                                                  |
| **GDPR 被遗忘权**              | `POST /api/v1/user/delete-data`：清除 KV 会话 + 指令前端清除 IndexedDB/LocalStorage                                                   |
| **合规埋点**                   | 默认 `ANALYTICS_PROVIDER=none`；开启后仅采集匿名行为数据，不采集 PII                                                                          |

### 7.4 可观测性与 SLO

| SLO 指标                      | 阈值                                            | 告警                            |
| --------------------------- | --------------------------------------------- | ----------------------------- |
| API P95 延迟                  | < 800ms                                       | 超出阈值 2 分钟内告警（Discord Webhook） |
| 核心排盘 API 错误率                | < 1%                                          | 同上                            |
| CF Workers Analytics Engine | `latency_ms` + `status_code` + `version` tags | Dashboard 可视化                 |

---

## 八、CI/CD 与发布流程

### 8.1 双环境部署流水线

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  develop 分支  │────▶│   Staging    │────▶│  内部测试     │
│  (PR 合并)    │     │  CF Preview  │     │              │
└──────────────┘     └──────────────┘     └──────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  main + v* tag │────▶│  Production  │────▶│  线上 aov.cc  │
│  (正式发布)    │     │  CF Pages    │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
```

**CI 流水线顺序**：

```
lint → type-check → 单测(120) → quality-gates 聚合器 → build → deploy
```

### 8.2 版本绑定链

```
引擎 git tag v0.3.0
    ↕
npm 包 @temposoul/core@0.3.0
    ↕
主仓 package.json overrides 精确锁定
    ↕
API meta.version
    ↕
CF Pages 部署记录（CF_PAGES_COMMIT_SHA）
    ↕
scripts/version-guard.sh 门禁校验
```

### 8.3 回滚方案

- 每门验收基于 git tag，回滚 = revert 对应批次提交
- npm 包不可撤回 → 0.3.0 发布前必须 `npm publish dry-run` + 黄金集全绿双重门禁
- CF Pages 一键回滚到上一版本（Dashboard 操作，< 2 分钟）

---

## 九、跨专家交接矩阵

| 提供方   | 接收方    | 交接内容                                                                 | 格式/路径                        |
| ----- | ------ | -------------------------------------------------------------------- | ---------------------------- |
| A → B | 前端     | 62 端点清单 + 黄金用例路径 + requestId 上报约定 + runtime-config 字段                | `docs/handoff/A-to-BCD.md`   |
| A → C | 内容     | prompt 契约结构 + guardrails.json 挂载点 + 证据字段 schema + archetype_key 生成位置 | 同上                           |
| A → D | DevOps | quality-gates CI 挂载方式 + version-binding 规范 + 限流桩接口 + requestId 日志格式  | 同上                           |
| B → D | DevOps | dist/ 构建产物 + `_redirects` 需求 + sourcemap(hidden)                     | `dist/` 目录                   |
| C → A | 引擎     | archetype_key 枚举清单（filter 白名单）+ 50 词禁用词表 JSON                        | `src/prompt/guardrails.json` |
| C → B | 前端     | 词库数据 + 内容级多语言词条 + AI 提示词链 + 合规文案 + 危机干预文案                            | JSON/SQLite                  |
| D → A | 引擎     | CI runner 环境（Node 22 + corepack）+ 部署流水线                              | `.github/workflows/`         |
| D → B | 前端     | AUTH_ENABLED + CSP 策略 + ANALYTICS_PROVIDER + 真实域名 + 错误追踪端点           | `window.TEMPOSOUL_CONFIG`    |

---

## 十、风险登记簿与技术债务

| #   | 风险/债务                          | 影响               | 缓解措施                            | 优先级  |
| --- | ------------------------------ | ---------------- | ------------------------------- | ---- |
| 1   | 已存在的 29+185 项未提交代码质量未知         | 收口时可能需重写         | git 止血后逐文件审查                    | 🔴 高 |
| 2   | 引擎版本 0.1.26→0.3.0 升级后前端类型可能不兼容 | 构建/运行时错误         | M0 完成后前端先跑 `tsc --noEmit` 全量检查  | 🔴 高 |
| 3   | 塔罗牌面美术版权未裁决                    | TarotSpread 无牌面图 | 初期占位图 + 文字描述；法务确认后替换            | 🟡 中 |
| 4   | BirthPlaceModal 城市数据源未确认       | 出生地选择可能无数据       | 先内置 5000 城市静态数据；与 A 协商 API      | 🟡 中 |
| 5   | 阿拉伯语 RTL 测试覆盖不足                | 布局可能错位           | 每个组件开发时同步测试 `dir="rtl"`         | 🟡 中 |
| 6   | SPA 无 SSR，SEO 爬虫可能仅抓首页         | 搜索引擎收录不全         | 初期靠 useSEO + sitemap；流量增长后评估预渲染 | 🟢 低 |
| 7   | IndexedDB 在 Safari 隐私模式下受限     | 历史记录可能丢失         | 检测可用性，不可用时回退 localStorage       | 🟢 低 |
| 8   | html2canvas 导出图片部分浏览器兼容性差      | 分享功能受限           | 回退为复制文字摘要                       | 🟢 低 |

---

## 十一、全局验收检查清单（Go-Live Checklist）

### 🔧 工程层

- [ ] `grep -ri "mingyu"` 全库零结果
- [ ] `node -v` ≥ 22.0.0；`pnpm -v` ≥ 11.9.0
- [ ] `pnpm build` 成功，零 mingyu 字样
- [ ] `pnpm lint` 零错误零警告
- [ ] `pnpm exec tsc --noEmit` 零错误

### ⚙️ 引擎层

- [ ] `npm view @temposoul/core version` = 0.3.0
- [ ] Meeus 黄金数据集全部断言通过
- [ ] 62 端点本地 wrangler dev 全部可调通
- [ ] quality-gates 聚合器 exitCode 语义正确
- [ ] MCP `tools/list` 返回 > 20 个工具

### 🎨 前端层

- [ ] Lighthouse Performance ≥ 90 / LCP < 2.5s / INP < 200ms / CLS < 0.1
- [ ] 七语种切换无键名裸露
- [ ] 键盘可完成全部核心流程（WCAG 2.1 AA）
- [ ] 信任引擎 T0-T6 全链路可视化验证通过
- [ ] PWA Installable 通过；Service Worker activated

### 🔒 安全与合规

- [ ] securityheaders.com 扫描 A- 级以上
- [ ] `ANALYTICS_PROVIDER=none` 时 Network 面板无第三方请求
- [ ] Stripe Webhook 幂等测试通过
- [ ] GDPR 数据删除 API 可清空 KV + 前端 Storage
- [ ] 全流程无恐吓性文案；免责声明常驻

### 🚀 部署层

- [ ] Staging 环境部署成功
- [ ] Production 部署成功
- [ ] `/api/v1/health` 返回 `meta.version` = 0.3.0
- [ ] SLO 告警 2 分钟内触发通知
- [ ] 回滚演练 < 2 分钟完成

---

> **本方案整合自四位专家的独立部署方案，消除了跨域冗余，统一了命名规范与接口契约，建立了清晰的里程碑依赖拓扑与验收门禁。程序员团队可按 M0.1 → M0.2 → M0.3 → M0.4 → M0 → M1 → M2 → M3 → M4 → M5 → M6 的顺序逐项施工，每个里程碑都有明确的"全部满足才过"的硬指标。无需任何额外解释，本方案即为唯一事实源。**

---

## 附录 A · 首屏视觉重设计 + "探索星空"功能方案（2026-09-13 豆包新增）

> 本节为首屏（落地页）视觉重设计与"探索星空"核心功能的方案思路，下一步进入细化。
> 设计资产目录：`docs/design/`（情绪板、v1–v14 样图、济南概念图 v1–v4、定稿提示词）。

### A.1 情感内核与设计哲学

用户（访问者）是一个对人生感到迷茫的人。他站在当下的起点、自己的城市，抬头看头顶这片星空——而他和他所在的城市在浩瀚宇宙中只是一粒微尘。命律要做的，是陪他在这浩瀚里探索自己生命的节律与规律。

**主题文案**：「探索命运的节律」。**品牌**：命律 TempoSoul。

### A.2 三阶段认知体验（核心设计模型）

用户进入首屏后按以下顺序产生感受，所有视觉层级都服务于这个递进：

| 阶段 | 用户看到 | 视觉对应 |
|---|---|---|
| **第一眼** | 整片宇宙星空，屏幕最底下一个人 | 星空占画面约 85%，人物只是一个小光点；近景台地是程序化抽象线稿，不绑定真实数据 |
| **第二眼** | 原来这个环境就是我所在的城市 | 中远景按经纬度画出真实山河轮廓（河流走向、湖泊形状、远山位置），轮廓对了用户自然认出来 |
| **第三眼** | 认出大明湖、泉标、黄河、千佛山 | 近景地标天际线按真实建筑轮廓和位置画，**不贴任何文字标签**，靠"眼熟"建立亲近感 |

关键原则：**第一眼永远只是一个人站在屏幕最下方中心仰望宇宙**；城市山河是他所处环境的隐喻（曲折、复杂），宇宙是浩瀚要去探索的对象。

### A.3 视觉风格定稿（经 v1–v14 迭代 + 用户参考图锁定）

- **整体**：纯黑深底（#000000）、青蓝色荧光（cyan glow）、HUD 蓝图线稿风格、极简、大量黑色负空间
- **构图**：高视角斜上方俯瞰；上部约 85% 星空，底部约 15% 地面
- **星空**：清晰星点（大小明暗有层次、亮星带星芒），肉眼可见星为主不杂乱；重要星座用极细暗青线连接；星空中标注星座与亮星名（白色极小字）
- **月亮/行星**：按真实天文状态渲染（月相准确）
- **地面三层**：
  - 近景（人物周围）：一个扭曲的矩形线稿台地，人物站在最底边正中心，近处无建筑
  - 中景：起伏山峦、河流、绿地的线稿过渡
  - 远景：城市天际线剪影（建筑轮廓、球形地标、湖泊、河流、远山），压暗做远景，不标地名
- **UI**：左上悬浮「命律 TempoSoul · 探索命运的节律」，右上「立即开始」玻璃按钮；无传统顶栏
- **禁止**：阴天雾气、过亮中心光源、实体山岩、蜿蜒发光道路、平面俯视地图感、满天噪点星

定稿提示词见 `docs/design/hero-prompt-v1.md`。

### A.4 真实星空技术方案

目标：用户打开页面看到的星空与他窗外此刻的夜空完全一致，可截图保存。

| 环节 | 方案 |
|---|---|
| 定位 | Cloudflare Pages Functions `request.cf` 自带经纬度/城市/时区（城市级精度）；"精确定位"按钮调浏览器 Geolocation 到街道级 |
| 星表 | Hipparcos 亮于 ~4.5 等恒星约 1500 颗（赤经/赤纬/星等/色指数），gzip ~50KB，路由级懒加载 |
| 计算 | JD(UTC) → 本地恒星时 → 每颗星地平坐标(alt/az)，只画 alt>0；复用 `@temposoul/core/calendar` 已有 JD/ΔT/时区能力 |
| 星座连线 | IAU 标准连线数据（~50 主要星座，~10KB）；星座素描线稿图案与名称标注 |
| 月亮/行星 | 月亮相位复用 core；行星位置 P2 补 |
| 渲染 | Canvas 2D，1500 星点 + 连线，60fps，零依赖（不引入 Three.js） |

### A.5 真实荧光地图技术方案

目标：不标文字，但用户能认出"这是我的城市"。

- **数据源**：OpenStreetMap 矢量要素（主干道、河流、湖泊、绿地、建筑轮廓、知名地标），ODbL 免费需署名
- **服务端**：Pages Functions 代理 Overpass API，按城市网格 key 存 CF KV 缓存（TTL 7 天），绕限速
- **透视**：不是平面俯视，是人眼高度向远方看的透视——近景台地放大清晰、中景山峦过渡、远景城市天际线压暗缩小
- **地标专属**：主要城市知名地标轮廓表（如济南：黄河、大明湖、泉城广场泉标、千佛山/泰山余脉），代码内置，不显示标签
- **OSM 中国数据稀疏降级**：要素不足时退化为"河流+主干道+山体轮廓"三层，宁可空也不画错

### A.6 "探索星空"功能思路（待细化）

首屏星景点进去后进入"探索星空"功能，展示用户此时此刻头顶的真实星空，规划包含：

1. **星座标注与图案**：每个可见星座都标注名称，并配对应的星座素描线稿图案（如人马座配半人马/人马造型的线稿），而非仅连线。
2. **星座说明**：每个星座附简短说明（来源神话、最佳观测时间、主要亮星）。
3. **关系关联分析**：分析该星空（用户出生/当前时间地点）与用户之间的关系——与命盘（八字/星盘/紫微）的对应、当季/当时当空星座与命主星曜的呼应、行星行运等。
4. **其他功能**：待补充（如星空截图保存、时间滑动回看历史星空、星图旋转交互、收藏星座、星座运势关联提示词生成等）。

> 本节为思路草案，下一步细化：数据结构、交互流程、与现有排盘体系（八字/星盘/紫微）的关联接口、文案与多语言。

### A.7 分期计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| **P0** | CF 取经纬度；精简星表+地平坐标计算（复用 core）；Canvas 真实星空；近景台地+人物光点；三阶段视觉骨架 | 济南此刻打开，星空与 Stellarium 对齐 |
| **P1** | Overpass 拉真实矢量要素 + KV 缓存；中景山河线稿；远景城市天际线剪影；方位朝北 | 轮廓能认出所在城市 |
| **P2** | 浏览器 GPS 精确定位；月亮/行星；星座名与线稿图案标注；"探索星空"功能页骨架 | 截图即真实夜空 |
| **P3** | 星座说明、与命盘的关系关联分析、星空截图/时间滑动等增强 | 见 A.6 细化 |

### A.8 设计资产索引（`docs/design/`）

- `moodboard-hero-v1.png` — 早期情绪板
- `mockup-hero-v1.png` … `mockup-hero-v14.png` — 首屏迭代样图
- `mockup-hero-jinan-v1.png` … `mockup-hero-jinan-v4.png` — 济南 2026-09-13 20:00 真实时空概念图
- `hero-prompt-v1.md` — 定稿图像生成提示词（含决策记录）

### A.9 新增功能：指定地点与时间 + 星空截图保存

首屏与"探索星空"支持用户主动指定地点和时间，而非只用实时位置：

1. **地点时间选择器**
   - 默认：实时（IP 定位 + 当前时间）
   - 可选：搜索城市/经纬度输入；时间滑块（过去/未来任意时刻，默认当前）
   - 切换后星空与地图同步按该地点时间重渲染
2. **全屏星空模式**
   - 用户可选择"只看星空"——隐藏地面地图、人物、UI，全屏纯净星图
   - 全屏下可保存高清星空图（Canvas 2x/3x 离屏渲染导出 PNG）
3. **组合截图**
   - 可保存"星空 + 地图缩影"整张图（当前首屏构图：上部星空 + 下部地面线稿），用于分享
4. **实现要点**
   - 截图导出：离屏 canvas 按 devicePixelRatio ×2 重绘（UI 文字用 DOM 合成或单独绘制），`toBlob('image/png')` 下载
   - 地点时间参数化：URL query `?lat=&lng=&t=`，可分享固定时空
   - 性能：时间滑动时按分钟级节流重算，不逐帧天文计算

### A.10 下一阶段整体部署方案（视觉重设计 + 探索星空）

**目标**：把首屏从现有粉紫工具页改造成"真实星空 + 城市线稿"沉浸首屏，并落地"探索星空"功能。

**技术路线**：
- 保持 React 19 + Vite 7 + Cloudflare Pages 现有栈；Canvas 2D 渲染，不引入 Three.js
- 天文计算下沉到 `@temposoul/core`（或新增 `sky` 子模块），与现有 calendar/历法层共用 JD/ΔT
- 星表、星座连线、地标轮廓数据走路由级懒加载，不进结果页，守住首页 gzip 预算
- 地图矢量数据服务端 Overpass 代理 + KV 缓存，前端只拿过滤后的 GeoJSON

**分期**：P0 真实星空骨架 → P1 荧光地图 → P2 交互/截图/GPS → P3 探索星空功能页与关系分析（见 A.7）。

**性能红线**：首屏 JS gzip 增量 ≤ 100KB（星表+连线数据）；星空渲染 60fps；尊重 `prefers-reduced-motion`。

**验收基线**（以济南 36.68°N/117.18°E 2026-09-13 20:00 为对照样本）：星空与 Stellarium 同时间地点一致；地面线稿能认出黄河/大明湖/南部山线。

### A.11 多线程任务方案

下一阶段拆为 7 个可并行工作线程，按依赖分两波：

**第一波（可立即并行，互不依赖）**

| 线程 | 负责 | 产出 |
|---|---|---|
| **T1 天文内核** | 精简星表提取（Hipparcos ≤4.5 等 ~1500 星：RA/Dec/mag/B-V）；地平坐标计算（JD→LST→alt/az）；IAU 星座连线数据；月亮相位/行星位置；单元测试 | `@temposoul/core/sky` 模块 + 黄金用例（济南 2026-09-13 20:00 星位） |
| **T2 星空渲染器** | Canvas 2D 星空：星点大小/颜色/星芒、极细星座连线、星座标签、月亮渲染；全屏模式；reduced-motion 降级 | 可复用 `SkyCanvas` 组件 |
| **T3 地图渲染器** | 人眼透视投影（朝北、近大远小）；OSM 矢量 GeoJSON → 荧光线稿；近景程序化扭曲台地；远景城市天际线剪影 | `TerrainCanvas` 组件 |
| **T4 服务端定位与数据** | Pages Functions 取 `request.cf`；Overpass 代理 + KV 缓存（TTL 7d）；城市地标轮廓表（济南起步）；地点/时间参数化 API | `/api/v1/sky?lat=&lng=&t=` 与 `/api/v1/terrain` 端点 |

**第二波（依赖第一波接口）**

| 线程 | 负责 | 依赖 |
|---|---|---|
| **T5 交互与UI** | 地点时间选择器；全屏星空切换；高清截图导出（星空/星空+地图）；新首屏布局（左上标题、右上按钮）；URL 参数分享 | T2/T3 组件接口、T4 API |
| **T6 星座内容** | 88 星座说明文案（神话/最佳观测/亮星）；星座素描线稿图案素材；多语言 | T1 星座清单 |
| **T7 关系关联分析** | 当空星座/行星与用户命盘（八字/星盘/紫微）的关联接口设计；行运/当季星象与命主星曜呼应的提示词拼装 | T1 星位数据、现有排盘 core |

**主控收口**：T1–T4 第一波合并 → T5–T7 第二波 → 首屏联调 → Lighthouse 复测 → 灰度上线。
**铁律**：所有天文计算必须有黄金用例对照；地图数据 ODbL 署名；不引入 Three.js；守住 gzip 红线。
